GEMINI_API_KEY=["put_your_fking_api_key_here,leave_blank_to_disable_gemini"]            #Optional, only paste your api key if you enabled it in config.py. A lot of api key is better than one.
OPENAI_API_KEY=["put-ur-fk-openai-key-here"]                                            #Optional, only paste your api key if you enabled it in config.py. A lot of api key is better than one.
GROQ_API_KEY=["put-ur-fk-groq-key-here"]                                                #Optional, only paste your api key if you enabled it in config.py. A lot of api key is better than one.                                 #Optional, only paste your api key if you enabled it in config.py. A lot of api key is better than one.
lm_studio_port="1234"                                                                   #Optional, only paste your lm studio port if you enabled it in config.py
lm_studio_ip="127.0.0.1"                                                                #Optional, only paste your lm studio ip if you enabled it in config.py
DISCORD_TOKEN="put-ur-fk-bot-token-here"
